# Required. Fine-grained PAT: Contents/Metadata Read; Traffic needs Administration: Read
GITHUB_TOKEN=github_pat_replace_me

# Required. Used by Postgres and the app. Pick a password and keep this file private.
POSTGRES_PASSWORD=replace_me

# Optional
# APP_TIMEZONE=UTC
# COLLECTION_PLANNER_FROM=10:00
# COLLECTION_PLANNER_TO=18:00
