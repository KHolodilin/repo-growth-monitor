databaseChangeLog:
  - changeSet:
      id: 005-watchers
      author: repo-growth-monitor
      changes:
        - sql:
            sql: |
              ALTER TABLE repository
                  ADD COLUMN watchers INTEGER NOT NULL DEFAULT 0;

              ALTER TABLE repository_daily_stats
                  ADD COLUMN watchers INTEGER NOT NULL DEFAULT 0;
